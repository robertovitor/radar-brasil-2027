name: Pesquisa editorial Radar

on:
  schedule:
    - cron: "45 * * * *"
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/pesquisa_editorial.py'
      - '.github/workflows/pesquisa-editorial.yml'

permissions:
  contents: write

concurrency:
  group: pesquisa-editorial-radar
  cancel-in-progress: false

jobs:
  pesquisar:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Registrar origem do gatilho
        run: |
          echo "pesquisa_direct_schedule=${{ github.event_name == 'schedule' }}"
          echo "trigger_event=${{ github.event_name }}"
          echo "triggered_at=$(date -u +%Y-%m-%dT%H:%M:%SZ)"

      - name: Executar pesquisa editorial nativa
        id: pesquisa
        env:
          AIRTABLE_TOKEN: ${{ secrets.AIRTABLE_TOKEN }}
        shell: bash
        run: |
          set +e
          python scripts/pesquisa_editorial_compat_v2.py
          code=$?
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Publicar inbox e telemetria da mesma execução
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'

          # Ambos os arquivos são gerados pela mesma pesquisa. Versioná-los juntos evita
          # deixar pesquisa-status.json sem stage antes do pull --rebase do inbox.
          git add editorial/inbox.json editorial/pesquisa-status.json

          if git diff --cached --quiet; then
            echo 'ERRO: pesquisa não alterou inbox nem pesquisa-status.json.'
            exit 2
          fi

          git commit -m 'Atualiza resultado da pesquisa editorial nativa'
          git pull --rebase origin main
          git push origin main

      - name: Falhar workflow se a pesquisa falhou
        if: steps.pesquisa.outputs.exit_code != '0'
        run: |
          echo "Pesquisa editorial falhou com código ${{ steps.pesquisa.outputs.exit_code }}. O status já foi persistido."
          exit 1
