name: Agendar Instagram via dispatch

on:
  schedule:
    - cron: "12 * * * *"
  workflow_dispatch:

permissions:
  actions: write
  contents: read

concurrency:
  group: instagram-native-scheduler
  cancel-in-progress: true

jobs:
  disparar:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Registrar gatilho do scheduler
        run: |
          echo "instagram_intermediate_scheduler_active=true"
          echo "trigger_event=${{ github.event_name }}"
          echo "triggered_at=$(date -u +%Y-%m-%dT%H:%M:%SZ)"

      - name: Disparar workflow oficial do Instagram
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          echo "Disparando publicar-instagram-automatico.yml via workflow_dispatch."
          gh api \
            --method POST \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "/repos/${GH_REPO}/actions/workflows/publicar-instagram-automatico.yml/dispatches" \
            -f ref=main
          echo "instagram_dispatch_requested=true"
